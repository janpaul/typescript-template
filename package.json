{
  "name": "primes",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "15.6.0",
    "prettier": "2.3.0",
    "typescript": "4.2.4"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf build",
    "format": "prettier --write .",
    "watch": "tsc --project . --watch",
    "start": "yarn build && node build/index.js"
  }
}
